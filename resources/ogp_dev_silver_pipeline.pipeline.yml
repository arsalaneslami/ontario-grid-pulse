resources:
  pipelines:
    silver_pipeline:
      name: ogp_${bundle.target}_silver_pipeline
      catalog: ${var.catalog}
      schema: silver
      serverless: true
      photon: true
      channel: CURRENT
      continuous: false
      libraries:
        - notebook:
            path: ../src/silver/silver_pipeline.py
