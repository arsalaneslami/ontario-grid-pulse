resources:
  pipelines:
    gold_pipeline:
      name: ogp_${bundle.target}_gold_pipeline
      catalog: ${var.catalog}
      schema: gold
      serverless: true
      photon: true
      channel: CURRENT
      continuous: false
      libraries:
        - notebook:
            path: ../src/gold/gold_pipeline.py
